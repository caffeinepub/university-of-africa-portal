{
  "frontend/src/pages/admin/FeeManagementPage.tsx": "Admin fee management page wrapped in RoleGuard, with actor-readiness checks, disabled submit button while connecting, success/error toasts via sonner, and instant cache invalidation after adding a fee type.",
  "frontend/src/pages/admin/HostelManagementPage.tsx": "Admin hostel applications page wrapped in RoleGuard, with actor-readiness checks, per-row processing state, approve/reject actions with success/error toasts via sonner, and instant cache invalidation.",
  "frontend/src/pages/admin/MessagingPage.tsx": "Admin messaging/announcements page wrapped in RoleGuard, with actor-readiness checks, disabled send button while connecting, success/error toasts via sonner, and a session-local sent messages history.",
  "frontend/src/pages/PaymentSuccessPage.tsx": "Payment success redirect page that verifies the Stripe session status, records the payment in the backend, and displays a success confirmation with the payment reference.",
  "frontend/src/pages/PaymentFailurePage.tsx": "Payment failure/cancellation page shown when Stripe checkout is cancelled, with links to return to the fee statement or dashboard.",
  "frontend/src/components/payments/StripeSetup.tsx": "Admin component that checks if Stripe is configured and shows a setup form (secret key + allowed countries) if not; hides itself once Stripe is already configured.",
  "frontend/src/pages/student/ResultsPage.tsx": "Student results page showing academic results grouped by semester with GPA calculations; fixes the `semResults` possibly-undefined TypeScript error by using explicit typed arrays.",
  "frontend/src/pages/student/TranscriptPage.tsx": "Print-ready academic transcript page showing all results by semester with GPA; fixes the `semResults` possibly-undefined TypeScript error by using explicit typed arrays.",
  "frontend/src/pages/parent/ParentDashboard.tsx": "Parent dashboard with sidebar, student-linking form, and linked student's academic results grouped by semester with GPA. Uses `useGetResultsForStudent` as a mutation (called imperatively after linking) and stores results in local state, fixing the TS2554 argument error.",
  "frontend/src/pages/student/PaymentHistoryPage.tsx": "Student payment history page with Naira-formatted totals and transaction table; receipt navigation fixed to use the valid /student/receipt/$paymentId route with params instead of search params.",
  "frontend/src/utils/currency.ts": "Utility module exporting formatNaira and formatNairaFromBackend functions using Intl.NumberFormat with en-NG locale for Nigerian Naira (₦) formatting with thousand separators.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file with all backend operations, now including a `useBootstrapAdmin` mutation that calls `actor.bootstrapAdmin(name, idNumber, email)` and invalidates `currentUserProfile`, `currentUserRole`, and `allUserProfiles` query keys on success. Also improved `extractErrorMessage` to handle the \"admin already exists\" error case.",
  "frontend/src/pages/admin/StudentManagementPage.tsx": "Admin student management page wrapped in RoleGuard, with actor-readiness checks, disabled submit button while connecting, success/error toasts via sonner, and instant cache invalidation after registering a student.",
  "frontend/src/pages/admin/ProgrammeManagementPage.tsx": "Admin course/programme management page wrapped in RoleGuard, with actor-readiness checks, disabled submit button while connecting, success/error toasts via sonner, and instant cache invalidation after adding a course.",
  "frontend/src/pages/student/FeeStatementPage.tsx": "Student fee statement page with Naira-formatted totals, fee schedule table with inline Pay Now button using Stripe checkout, and recent payments list. Updated `handlePayNow` to pass the correct `{ items, successUrl, cancelUrl }` object shape to `useCreateCheckoutSession`, fixing the TS2345 type error.",
  "frontend/src/pages/student/ReceiptPage.tsx": "Print-ready official payment receipt page using useParams from the /student/receipt/$paymentId route, displaying Naira-formatted amount and navigating back to /student/payments.",
  "frontend/src/components/layout/Navigation.tsx": "Navigation updated with a public \"Portals\" nav link and button for unauthenticated users pointing to /portal; authenticated user navigation remains unchanged with role-based dropdown links.",
  "frontend/src/pages/student/StudentDashboard.tsx": "Student dashboard with sidebar, stats cards, and services grid. Now shows a full skeleton loading state while the profile query is in progress and an error state with retry button if the fetch fails.",
  "frontend/src/pages/staff/StaffDashboard.tsx": "Staff dashboard with sidebar, stats cards (students, courses, results posted), and quick-access services grid. Fixed TypeScript route errors by pointing all staff sidebar/service links to the valid `/staff` route (since `/staff/students`, `/staff/results`, `/staff/courses` are not registered routes).",
  "frontend/src/pages/admin/AdminDashboard.tsx": "Admin dashboard that now detects when no profile exists and renders `AdminSetupCard` + `AdminProfileCard` inline instead of the main dashboard content; when a profile is present, `AdminProfileCard` is shown in the header area alongside the welcome message.",
  "frontend/src/components/layout/Layout.tsx": "Root layout shell updated to detect the current route path and extract a `defaultRole` from role-specific login pages (/login/student, etc.), passing it to ProfileSetupModal so new users get the correct role pre-selected.",
  "frontend/src/App.tsx": "Main app router updated with five new public routes: /portal (PortalSelectionPage) and /login/student, /login/staff, /login/admin, /login/parent (each rendering RoleLoginPage with the corresponding role prop).",
  "frontend/src/components/auth/ProfileSetupModal.tsx": "Profile setup modal updated to accept an optional `defaultRole` prop; when provided, the role field is pre-filled and shown as read-only (locked), preventing the user from changing it; existing form validation and submission logic unchanged.",
  "frontend/src/pages/admin/ResultsManagementPage.tsx": "Admin results management page wrapped in RoleGuard, with actor-readiness checks, course/grade dropdowns, disabled submit while connecting, success/error toasts via sonner, and instant cache invalidation after posting a result.",
  "frontend/src/pages/admin/AdmissionsManagementPage.tsx": "Admin admissions management page wrapped in RoleGuard, with actor-readiness checks, per-row processing state, admit/reject actions with success/error toasts via sonner, and instant cache invalidation.",
  "frontend/src/components/auth/RoleGuard.tsx": "Role-based access guard component that checks authentication state, actor readiness, and user profile role before rendering children; shows loading skeletons, login prompts, or access-denied screens as appropriate.",
  "frontend/src/utils/principal.ts": "Utility module exporting `formatPrincipal(principal: Principal | string): string` that truncates a principal string to show the first two dash-separated segments and the last segment with ellipsis in the middle (e.g., `xxxxx-xxxxx-...-cai`).",
  "frontend/src/components/admin/AdminSetupCard.tsx": "Card component with a form (name, staff ID, email) that calls `useBootstrapAdmin` to claim the first admin account; shows inline error messages, a loading spinner during submission, and redirects to `/admin` with a success toast on completion.",
  "frontend/src/components/admin/AdminProfileCard.tsx": "Card component that displays the authenticated user's Internet Identity principal (formatted and copyable) alongside their stored profile name, email, and ID number fetched live from the backend; shows a prompt if no profile exists yet.",
  "frontend/src/pages/admin/AdminSetupPage.tsx": "Dedicated admin setup page at `/admin/setup` with university branding, `AdminSetupCard` for claiming the admin account, and `AdminProfileCard` showing the principal; accessible without role restrictions so the first user can bootstrap admin.",
  "frontend/src/pages/PortalSelectionPage.tsx": "New public page at /portal displaying four portal entry cards (Student, Staff, Admin, Parent) with descriptions and feature lists; clicking any card navigates to the corresponding role-specific login page (/login/{role}).",
  "frontend/src/pages/auth/RoleLoginPage.tsx": "Parameterized role-specific login page component accepting a `role` prop; displays role branding on the left panel and an Internet Identity login button on the right; handles post-login redirect to the correct dashboard, mismatched-role warnings, and triggers the ProfileSetupModal for new users via the Layout.",
  "frontend/src/pages/HomePage.tsx": "Homepage updated so portal cards route unauthenticated visitors to /login/{role} pages and authenticated users with a profile directly to their dashboards; hero section \"Access Portal\" button now links to /portal for unauthenticated users.",
  "frontend/src/components/layout/Footer.tsx": "Footer updated to use navigate() with smart portal links — unauthenticated visitors are routed to /login/{role} pages while authenticated users with profiles go directly to their dashboards; also added an \"All Portals\" quick link."
}