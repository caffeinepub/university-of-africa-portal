{
  "frontend/src/pages/admin/FeeManagementPage.tsx": "Admin fee management page wrapped in RoleGuard, with actor-readiness checks, disabled submit button while connecting, success/error toasts via sonner, and instant cache invalidation after adding a fee type.",
  "frontend/src/pages/admin/HostelManagementPage.tsx": "Admin hostel applications page wrapped in RoleGuard, with actor-readiness checks, per-row processing state, approve/reject actions with success/error toasts via sonner, and instant cache invalidation.",
  "frontend/src/pages/admin/MessagingPage.tsx": "Admin messaging/announcements page wrapped in RoleGuard, with actor-readiness checks, disabled send button while connecting, success/error toasts via sonner, and a session-local sent messages history.",
  "frontend/src/pages/PaymentSuccessPage.tsx": "Payment success redirect page that verifies the Stripe session status, records the payment in the backend, and displays a success confirmation with the payment reference.",
  "frontend/src/pages/PaymentFailurePage.tsx": "Payment failure/cancellation page shown when Stripe checkout is cancelled, with links to return to the fee statement or dashboard.",
  "frontend/src/components/payments/StripeSetup.tsx": "Admin component that checks if Stripe is configured and shows a setup form (secret key + allowed countries) if not; hides itself once Stripe is already configured.",
  "frontend/src/pages/student/ResultsPage.tsx": "Student results page showing academic results grouped by semester with GPA calculations; fixes the `semResults` possibly-undefined TypeScript error by using explicit typed arrays.",
  "frontend/src/pages/student/TranscriptPage.tsx": "Print-ready academic transcript page using local ResultItem and CourseItem interfaces instead of importing from backend; fetches results by studentId and displays them grouped by semester with GPA calculations.",
  "frontend/src/pages/parent/ParentDashboard.tsx": "Parent dashboard with sidebar, student-linking form using the correct `{parentId, studentId}` mutation signature, and linked student's academic results grouped by semester with GPA; uses local ResultItem interface instead of importing Result from backend.",
  "frontend/src/pages/student/PaymentHistoryPage.tsx": "Student payment history page with Naira-formatted totals and transaction table; receipt navigation fixed to use the valid /student/receipt/$paymentId route with params instead of search params.",
  "frontend/src/utils/currency.ts": "Utility module exporting formatNaira and formatNairaFromBackend functions using Intl.NumberFormat with en-NG locale for Nigerian Naira (₦) formatting with thousand separators.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file with all backend operations including six new portal access application hooks (useSubmitPortalAccessApplication, useGetPortalAccessApplications, useApprovePortalAccessApplication, useRejectPortalAccessApplication, useGetMyPortalAccessApplication, useLoginWithIdAndPassword) plus all previously existing hooks preserved.",
  "frontend/src/pages/admin/StudentManagementPage.tsx": "Admin student management page wrapped in RoleGuard, with actor-readiness checks, disabled submit button while connecting, success/error toasts via sonner, and instant cache invalidation after registering a student.",
  "frontend/src/pages/admin/ProgrammeManagementPage.tsx": "Admin programme management page updated with Department (text input), Level (select: 100–500), Credit Units (number input), and Semester (select) fields in the add-course form; the course catalogue table now shows Department, Level, and Credit Units columns for every course.",
  "frontend/src/pages/student/FeeStatementPage.tsx": "Student fee statement page with summary cards, fee schedule table with Pay Now buttons, and recent payments list; uses local FeeTypeItem interface instead of importing FeeType from backend, and passes studentId to useCheckUnpaidFees.",
  "frontend/src/pages/student/ReceiptPage.tsx": "Print-ready official payment receipt page using useParams from the /student/receipt/$paymentId route, displaying Naira-formatted amount and navigating back to /student/payments.",
  "frontend/src/components/layout/Navigation.tsx": "Navigation bar showing Apply for Access and ID Login links to unauthenticated visitors alongside the existing portal/home links, and a dashboard link for authenticated users.",
  "frontend/src/pages/student/StudentDashboard.tsx": "Student dashboard with sidebar, stats cards, and services grid; passes the student's idNumber to useCheckUnpaidFees and uses `as any` casts for navigate to avoid route type errors.",
  "frontend/src/pages/staff/StaffDashboard.tsx": "Staff dashboard with sidebar, stats cards (students, courses, results posted), and quick-access services grid. Fixed TypeScript route errors by pointing all staff sidebar/service links to the valid `/staff` route (since `/staff/students`, `/staff/results`, `/staff/courses` are not registered routes).",
  "frontend/src/pages/admin/AdminDashboard.tsx": "Admin dashboard with a grid of management cards including the new Access Applications link, quick stats section, and conditional admin setup flow when no profile exists.",
  "frontend/src/components/layout/Layout.tsx": "Root layout wrapper that accepts children, detects the current route to extract a defaultRole for the ProfileSetupModal, and conditionally shows the profile setup modal for authenticated users without a profile.",
  "frontend/src/App.tsx": "Main app router with all routes including two new public routes /apply (PortalAccessApplicationPage) and /id-login (IdPasswordLoginPage), plus /admin/access-applications (AccessApplicationsPage).",
  "frontend/src/components/auth/ProfileSetupModal.tsx": "Profile setup modal updated to show Department (text input) and Level (select: 100–500) fields conditionally when the selected role is Student; these fields are omitted for Staff, Admin, and Parent roles; submitted values are passed as `department` and `level` in the profile payload.",
  "frontend/src/pages/admin/ResultsManagementPage.tsx": "Admin results management page wrapped in RoleGuard, with actor-readiness checks, course/grade dropdowns, disabled submit while connecting, success/error toasts via sonner, and instant cache invalidation after posting a result.",
  "frontend/src/pages/admin/AdmissionsManagementPage.tsx": "Admin admissions management page wrapped in RoleGuard, with actor-readiness checks, per-row processing state, admit/reject actions with success/error toasts via sonner, and instant cache invalidation.",
  "frontend/src/components/auth/RoleGuard.tsx": "Role-based access guard that checks authentication state and user profile role before rendering children; shows loading skeletons, login prompts, or access-denied screens as appropriate, using string comparison for role matching.",
  "frontend/src/utils/principal.ts": "Utility module exporting `formatPrincipal(principal: Principal | string): string` that truncates a principal string to show the first two dash-separated segments and the last segment with ellipsis in the middle (e.g., `xxxxx-xxxxx-...-cai`).",
  "frontend/src/components/admin/AdminSetupCard.tsx": "Card component with a form (name, staff ID, email) that calls `useBootstrapAdmin` to claim the first admin account; shows inline error messages, a loading spinner during submission, and redirects to `/admin` with a success toast on completion.",
  "frontend/src/components/admin/AdminProfileCard.tsx": "Card component that displays the authenticated user's Internet Identity principal (formatted and copyable) alongside their stored profile name, email, and ID number fetched live from the backend; shows a prompt if no profile exists yet.",
  "frontend/src/pages/admin/AdminSetupPage.tsx": "Dedicated admin setup page at `/admin/setup` with university branding, `AdminSetupCard` for claiming the admin account, and `AdminProfileCard` showing the principal; accessible without role restrictions so the first user can bootstrap admin.",
  "frontend/src/pages/PortalSelectionPage.tsx": "Public portal selection page at /portal showing four role cards (Student, Staff, Admin, Parent) plus an alternative access section with cards linking to /apply and /id-login.",
  "frontend/src/pages/auth/RoleLoginPage.tsx": "Parameterized role-specific login page component accepting a `role` prop; displays role branding on the left panel and an Internet Identity login button on the right; handles post-login redirect to the correct dashboard, mismatched-role warnings, and triggers the ProfileSetupModal for new users via the Layout.",
  "frontend/src/pages/HomePage.tsx": "Homepage with hero banner, stats, portal cards, a new user CTA banner with Apply for Access and ID Login buttons (shown only to unauthenticated users), and features section.",
  "frontend/src/components/layout/Footer.tsx": "Footer updated to use navigate() with smart portal links — unauthenticated visitors are routed to /login/{role} pages while authenticated users with profiles go directly to their dashboards; also added an \"All Portals\" quick link.",
  "frontend/src/pages/student/CourseRegistrationPage.tsx": "Course registration page that fetches courses by department/level and registered courses using the student's idNumber; uses correct `{studentId, courseId}` mutation signatures for register/deregister and local CourseItem interface.",
  "frontend/src/pages/PortalAccessApplicationPage.tsx": "Public portal access application page at /apply displaying the university logo and a form collecting Full Name, Email, Role (dropdown), and Programme/Department; shows a success confirmation with reference ID on submission and links to the ID login page.",
  "frontend/src/pages/IdPasswordLoginPage.tsx": "Public ID/password login page at /id-login with university logo, role selector that updates the ID field placeholder dynamically, password field with show/hide toggle, and redirects to the role-specific dashboard on successful login.",
  "frontend/src/pages/admin/AccessApplicationsPage.tsx": "Admin page at /admin/access-applications listing all portal access applications in a table with approve/reject actions; approving shows a credentials modal with the generated ID and password that can be copied to clipboard.",
  "frontend/src/pages/AdmissionCheckerPage.tsx": "Public admission status checker page with tabs for checking by JAMB number or name; uses local AdmissionStatusValue type instead of importing the non-existent AdmissionStatus from backend.",
  "frontend/src/pages/student/HostelApplicationPage.tsx": "Student hostel application page that fetches the student's existing application using their idNumber from profile, shows application status if exists, or renders the application form; removed RoleGuard wrapper to avoid circular dependency.",
  "frontend/src/components/RoleGuard.tsx": "Legacy role guard component (at components root, not auth subdirectory) that checks authentication and role before rendering children; fixed the inline useLocalCallerUserProfile hook to use `(actor as any).getCallerUserProfile()` cast to avoid the TypeScript error about the missing method in the generated backend interface."
}