{
  "frontend/src/pages/admin/FeeManagementPage.tsx": "Admin fee management page with a form to add fee types and a table of all fees. Now includes an `authError` state and an `Alert` banner that shows a clear re-authentication message when the backend returns an authorization error, with the error cleared on retry or field change.",
  "frontend/src/pages/admin/HostelManagementPage.tsx": "Admin hostel applications page with search/filter and approve/reject actions. Now includes authorization error detection with a dismissible `Alert` banner prompting re-authentication when the backend rejects the status update.",
  "frontend/src/pages/admin/MessagingPage.tsx": "Admin messaging page with a compose form for broadcast announcements and a session-local list of sent messages. Now includes authorization error detection with a dismissible `Alert` banner and clear re-authentication guidance.",
  "frontend/src/pages/PaymentSuccessPage.tsx": "Payment success redirect page that verifies the Stripe session status, records the payment in the backend, and displays a success confirmation with the payment reference.",
  "frontend/src/pages/PaymentFailurePage.tsx": "Payment failure/cancellation page shown when Stripe checkout is cancelled, with links to return to the fee statement or dashboard.",
  "frontend/src/components/payments/StripeSetup.tsx": "Admin component that checks if Stripe is configured and shows a setup form (secret key + allowed countries) if not; hides itself once Stripe is already configured.",
  "frontend/src/pages/student/ResultsPage.tsx": "Student results page showing academic results grouped by semester with GPA calculations; fixes the `semResults` possibly-undefined TypeScript error by using explicit typed arrays.",
  "frontend/src/pages/student/TranscriptPage.tsx": "Print-ready academic transcript page showing all results by semester with GPA; fixes the `semResults` possibly-undefined TypeScript error by using explicit typed arrays.",
  "frontend/src/pages/parent/ParentDashboard.tsx": "Parent dashboard with sidebar, student-linking form, and linked student's academic results grouped by semester with GPA. Now shows skeleton loading and error states while the profile is being fetched.",
  "frontend/src/pages/student/PaymentHistoryPage.tsx": "Student payment history page with Naira-formatted totals and transaction table; receipt navigation fixed to use the valid /student/receipt/$paymentId route with params instead of search params.",
  "frontend/src/utils/currency.ts": "Utility module exporting formatNaira and formatNairaFromBackend functions using Intl.NumberFormat with en-NG locale for Nigerian Naira (₦) formatting with thousand separators.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file with all backend operations and error-handling helpers. Restores the previously missing `useCheckAdmissionStatus`, `useCheckAdmissionStatusByName`, and `useGetStripeSessionStatus` exports that are required by AdmissionCheckerPage and PaymentSuccessPage.",
  "frontend/src/pages/admin/StudentManagementPage.tsx": "Admin student management page with registration form and searchable student table. Now includes authorization error detection with a dismissible `Alert` banner and clear re-authentication guidance when the backend rejects the mutation.",
  "frontend/src/pages/admin/ProgrammeManagementPage.tsx": "Admin course/programme management page with add-course form and course catalogue table. Now includes authorization error detection with a dismissible `Alert` banner prompting re-authentication when the backend rejects the mutation.",
  "frontend/src/pages/student/FeeStatementPage.tsx": "Student fee statement page with Nigerian Naira formatting, paid/outstanding summary cards, fee schedule table with inline Pay Now button using Stripe checkout, and recent payments list — all using valid routes.",
  "frontend/src/pages/student/ReceiptPage.tsx": "Print-ready official payment receipt page using useParams from the /student/receipt/$paymentId route, displaying Naira-formatted amount and navigating back to /student/payments.",
  "frontend/src/components/layout/Navigation.tsx": "Sticky header navigation with role-based dropdown menu. Now shows a skeleton loading state while the profile is being fetched after login, and falls back to a logout-only button if the profile fetch fails.",
  "frontend/src/pages/student/StudentDashboard.tsx": "Student dashboard with sidebar, stats cards, and services grid. Now shows a full skeleton loading state while the profile query is in progress and an error state with retry button if the fetch fails.",
  "frontend/src/pages/staff/StaffDashboard.tsx": "Staff dashboard with sidebar, stats cards (students, courses, results posted), and quick-access services grid. Fixed TypeScript route errors by pointing all staff sidebar/service links to the valid `/staff` route (since `/staff/students`, `/staff/results`, `/staff/courses` are not registered routes).",
  "frontend/src/pages/admin/AdminDashboard.tsx": "Admin dashboard with sidebar, stats cards (students, courses, fee types, pending admissions), and management tools grid. Now shows skeleton loading and error states while the profile is being fetched.",
  "frontend/src/components/layout/Layout.tsx": "Root layout shell rendering Navigation, the router Outlet, and the ProfileSetupModal. Passes `open={showProfileSetup}` to satisfy the required `open` prop on ProfileSetupModal.",
  "frontend/src/App.tsx": "Main app entry with TanStack Router setup. Fixed the root route to use `component: Layout` directly (since Layout now renders `<Outlet />` internally) instead of wrapping `<Layout><Outlet/></Layout>` which caused a TS2559 children mismatch.",
  "frontend/src/components/auth/ProfileSetupModal.tsx": "Profile setup modal that collects name, role, ID number, and email from a newly logged-in user. Uses the updated `useSaveCallerUserProfile` mutation (which now also assigns the `#user` access-control role) so that admin operations work immediately after profile creation.",
  "frontend/src/pages/admin/ResultsManagementPage.tsx": "Admin results management page with a form to post results (student, course, semester, grade, score dropdowns) and a full results table. Now includes authorization error detection with a dismissible `Alert` banner prompting re-authentication.",
  "frontend/src/pages/admin/AdmissionsManagementPage.tsx": "Admin admissions management page with search/filter and admit/reject actions. Now includes authorization error detection with a dismissible `Alert` banner and clear re-authentication guidance when the backend rejects the status update."
}